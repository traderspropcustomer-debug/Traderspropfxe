# TradersProp Client-Website Website

This is a separate website package. Configure its own HTTPS domain and environment variables before production deployment.

Client portal should communicate only with the approved backend API over HTTPS.
