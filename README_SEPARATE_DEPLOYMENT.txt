TradersProp CLIENT WEBSITE

This is a standalone website package separated from the original TradersProp portal.

Deploy this folder independently. Check config.js and set TRADERSPROP_API_BASE to the correct HTTPS backend URL before production deployment.
